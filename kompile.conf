# Kompile configuration - simple KEY=VALUE format

# Compiler selection
DEFAULT_COMPILER=gcc
CLANG_COMPILER=clang

# Common compilation flags (comma-separated)
COMMON_FLAGS=-std=c99,-pedantic,-Wall,-Wextra,-Werror,-O2,-g,-Wshadow,-Wconversion

# Compiler-specific flags (comma-separated)
GCC_FLAGS=-flto
CLANG_FLAGS=-Weverything

# Default behavior
DEFAULT_EXECUTE=1
OUTPUT_DIR=.
